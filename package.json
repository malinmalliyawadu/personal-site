{
  "name": "personal-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "gh-pages -b gh-pages -d dist",
    "generate-cv": "mkdir -p public/cv && cd cv && docker run --rm -v \"$(pwd):/data\" -w /data texlive/texlive lualatex Malin_CV.tex && cp Malin_CV.pdf ../public/cv/"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.15.9",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^6.4.0",
    "eslint": "^8.47.0",
    "eslint-plugin-astro": "^0.28.0",
    "gh-pages": "^6.0.0"
  }
}
